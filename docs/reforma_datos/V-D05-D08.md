# Verificación adversaria · D-05, D-06, D-07, D-08 (13-sep-2026)

> Para: sesión · Estado: informe fechado · Sustituido por: —

Postura: refutar. Cada dato sobrevive solo con el árbol delante y con una reproducción ejecutada.
Árbol: `main` en `3482415`. El repositorio no se tocó (`git status --short` vacío al cerrar).
Scripts y salidas, todos en esta carpeta: `v_d05_d06.js` → `v_d05_d06.salida.txt` · `v_d07.js` →
`v_d07.salida.txt` · `v_d08.js` → `v_d08.salida.txt` · `v_lenguaje_d05_d08.js` →
`v_lenguaje_d05_d08.salida.txt`. Usan las funciones REALES (`detalleAdjudicatario`, `detalleEntidad`,
`claveCanonica`, `socioQueAlcanza`, `socioPorProceso`, `filtrarProcesosVisibles`, `bloqueAdjudicatarios`
y `pintarAdjudicatario` recortadas de `public/app.js` con el mismo idioma que `tests/e2e.js`) sobre un
Redis en memoria; `fetch` va anulado (las fuentes vivas no se llaman). La cerca de lenguaje es la real
(`lib/lenguaje_pantalla.js`) más la lista de jerga que la suite usa en las razones de socio.

Resumen: **D-05 con condiciones · D-06 con condiciones · D-07 con condiciones · D-08 confirmado.**

---

## D-05 · La cabecera del perfil dice qué sostiene cada cifra — CON CONDICIONES

**1) Fuente — existe.** La respuesta de `detalleAdjudicatario` (`lib/competencia_detalle.js:660-690`)
publica `total_ganados` (:667), `valor_adjudicado_cop` = `null` si ninguna fila trae valor (:668),
`procesos_con_valor` (:669), `identificacion` = `null` cuando la clave es `n:` (:666) y `que_es`
(:682-688) con la cota. El tipo del identificador se rotula en :642-645: `nit` solo si salió de
`nit_del_proveedor_adjudicado`/`adjudicatario_nit`; `codigo_secop` para `codigoproveedor`; `documento`
para `documento_proveedor`. Un valor «0» es «sin dato» y no suma (:599-601).

**Ejecutado** (`v_d05_d06.salida.txt`, `detalleAdjudicatario` real, `usarCache:false`):
- A por NIT (15 ganados, 1 con valor «0»): `total_ganados=15 · procesos_con_valor=14 ·
  valor_adjudicado_cop=12600000000 · identificacion={"tipo":"nit","valor":"900123456"}`.
- B solo por nombre: `identificacion=null · procesos_con_valor=4 · valor=3600000000`.
- C solo `codigoproveedor`: `identificacion={"tipo":"codigo_secop","valor":"123456"}`.
- D todo con valor «0»: `procesos_con_valor=0 · valor_adjudicado_cop=null` (sin dato, no cero).

**Lo que pinta hoy `pintarAdjudicatario` (real, recortada de `public/app.js:3344-3380`)**, cabecera
del caso A: «CONSTRUCTORA DEL TOLIMA SAS NIT 900123456 15 contratos en 3 entidades · $12600 M ·
último: 2025-09-18 …». No aparece «14 de 15» en ninguno de los cuatro casos (`procesos_con_valor`
viaja y no se pinta: `app.js:3364-3368` solo imprime `total_ganados`, `nEnt`, el valor o «valor sin
dato» y «último»). En B la cabecera es «CONSORCIO VIAL 2025 4 contratos en 1 entidad · $3600 M …»:
ninguna línea de identidad (`app.js:3349-3353` da `""` con `identificacion null`). (En la salida, la
marca «línea de identidad SÍ («NIT del»)» del caso B es un falso positivo de mi regex: casa con «NIT
del ganador» del motivo de la baja media, no con una línea de identidad.) `que_es` va en el último
`<p>` gris (`app.js:3379`): confirmado que la cota vive solo al pie.

**2) Verdad.**
- «$28.400 M en 14 de 15 contratos con valor publicado»: 14 = `procesos_con_valor`, 15 =
  `total_ganados`; el total suma SOLO las filas con valor (:601) → es cierto que 14 sostienen la cifra.
  Sin dato ≠ cero: respetado en el servidor. **Condición**: la rama `valor_adjudicado_cop == null`
  (caso D: 3 ganados, 0 con valor) hoy dice «valor sin dato» (`app.js:3367`); la línea nueva no
  puede degenerar en «$0 en 0 de 3». Propuesta: «valor sin dato en sus 3 contratos».
- La cifra en pantalla la escribe `fmtCorto` (`app.js:2480-2487`): imprime «$28.400M» (sin espacio)
  y «No definida» para 0 — el 0 nunca llega porque el servidor manda `null`. El CORTO «$28.400 M» es
  una transcripción a mano: la literal sale de `fmtCorto`.
- «SECOP no publica el NIT de esta empresa: se identifica por el nombre **tal como lo escribe la
  entidad**»: la primera mitad tiene precedente en el propio árbol (motivo del servidor
  `competencia_detalle.js:655`, título `app.js:2935`); la segunda **no tiene fuente**: el nombre sale
  de `nombre_del_proveedor`/`adjudicatario_nombre`/… (`lib/indice_competencia.js:109-111`) y nada en
  el árbol dice quién lo escribe. El precedente en pantalla es «tal como lo publica el dataset»
  (`app.js:2936`). Propuesta: «…se identifica por el nombre tal como lo publica SECOP».
- «Solo lo que la aplicación sigue: obra y afines, procesos competitivos, desde enero de 2024»:
  sostenido por `DESDE_HISTORICO = "2024-01"` (`lib/handlers/procesos/sync.js:105`, decisión del
  dueño del 15-ago-2026) y por el prefiltro de ingesta (`sync.js:29-40`: `modalidad_competitiva`
  de `lib/filtros.js:242-248` y `admisibleParaIngesta`: UNSPSC de servicios/obra —segmentos 70-95—
  o de una familia que un RUP inscribe, o objeto textualmente de obra). Coincide con `que_es`
  (:682-683 «procesos competitivos de obra y servicios afines adjudicados desde 2024»). Cierto.
- **Hermano vivo**: la línea de identidad solo cubre `identificacion === null`. El caso C
  (`codigo_secop`, el GPS S.A.S de producción que la memoria cita) tampoco tiene NIT publicado y la
  cabecera hoy dice «Cód. SECOP 123456» sin explicación; el `title` de `app.js:2935` ya trae el texto
  («Código interno de proveedor en SECOP: el dataset no publica el NIT de esta empresa»), y un `title`
  no existe en el teléfono. Condición: la misma línea para `codigo_secop` y `documento`.
- Dos conteos parecidos en la misma cabecera: «14 de 15 contratos con valor publicado»
  (`procesos_con_valor`) y, justo debajo, «14 procesos ganados con presupuesto y valor adjudicado»
  (`baja_media.n`, `app.js:3337`). En el perfil por nombre divergen a propósito (B: «4 de 4 con
  valor» y `n=0` con motivo «SECOP no publica el NIT…»): las dos son ciertas y las redacciones son
  distintas. Condición: no unificar ni acercar los nombres (regla dura).

**3) Memoria.** «Remates «R4-remates-inteligencia» de la ola 2 · B9a-H1/H2/H3, B9b-H1/H2/H3/H4/H5
(6-sep-2026)»: B9b-H2 decidió que el «hay X» de la baja media NO cuente `procesos_con_valor` («la
cifra que falta no es la que bloquea») — D-05 usa `procesos_con_valor` como base del TOTAL, no como
motivo de la baja: compatible, y la condición anterior lo mantiene separado. «Lote
«B9b-competencia-departamento» de la consultoría del 4-sep · M-COMP-01, M-DGF-08 (6-sep-2026)»: la
cabecera del perfil ya lleva la baja media con su n; D-05 añade tres líneas encima. Ninguna decisión
escrita se desmiente.

**4) Costo — clic, real.** `op=competidor` (`lib/handlers/inteligencia/detalle.js:172-173`,
`vista=adjudicatario`) es una lectura; D-05 no añade comandos (30 comandos en la corrida, iguales
con o sin cabecera nueva).

**5) Lenguaje.** Las cuatro líneas pasan `tuteoEn`, `VOSEO_RE`, `RE_EMOJI_UI` y la lista de jerga
(`v_lenguaje_d05_d08.salida.txt`). Observación: el `que_es` que hoy va al pie contiene «corpus»
(jerga interna); si D-05 lo conserva al pie, no es un texto nuevo, pero si lo reescribe, sin «corpus».

**6) ¿Ya existe?** El nombre y la identidad (`NIT 900123456`) ya se pintan (`app.js:3361-3362`); el
«N contratos en M entidades · valor · último» también (:3364-3368). Lo nuevo es exactamente lo que la
ficha dice: la base del valor, la línea de identidad para el `n:` y la cota arriba. Cerraduras que
siguen en pie y hay que respetar: `tests/e2e.js:12924-12928` (`entidades[0].entidad`,
`identificacion`, y **`que_es` debe seguir diciendo «cota inferior»**: :12928), `:3232-3240` (perfil
por nombre: 6 ganados, `procesos_con_valor 6`, motivo con «NIT» y sin «hay 0»).

**Veredicto: con condiciones.** (1) rama `valor null` → «valor sin dato en sus N contratos», jamás
«$0 en 0 de N»; (2) la misma línea de identidad para `codigo_secop`/`documento`; (3) «tal como lo
escribe la entidad» → «tal como lo publica SECOP» (sin fuente para lo primero); (4) la cifra sale de
`fmtCorto`, no se transcribe; (5) mantener separadas las dos bases (valor publicado / presupuesto y
valor adjudicado); (6) `que_es` conserva «cota inferior» (cerradura :12928) aunque la cota suba.

---

## D-06 · En esta entidad: N de sus M contratos — CON CONDICIONES

**1) Fuente — existe, con una salvedad de dónde vive la regla.** `entidades[]`
(`competencia_detalle.js:670-681`) trae `entidad, ganados, valor_adjudicado_cop, procesos_con_valor,
ultima_adjudicacion`, agrupado por `lic.entidad` CRUDO (:593-595). `claveCanonica` existe en
`lib/indice_competencia.js:280` (exportada en :1242) y es la que agrupa el detalle de la entidad
(`competencia_detalle.js:79-80` y :213 `normalizar(lic.entidad) !== buscada`); el nombre más
frecuente se elige en :283-285. **`claveCanonica` no existe en el navegador**: ejecutado, ningún
`public/*.js` la contiene, y los normalizadores que hay (`app.js:5533`, `:7361`) son otros. La ficha
dice «el modal compara con `claveCanonica`»: tal cual no se puede sin copiar la regla en `app.js`
(prohibido: «no reescribir una regla que existe»). Lo que sí existe: `detalleEntidad` publica
`entidad_normalizada` = `claveCanonica(pedida)` (:490) al modal de la entidad. Condición: el
servidor agrupa `entidades[]` por `claveCanonica` y publica `clave` por fila; el navegador compara
cadenas publicadas.

**Ejecutado** (`v_d05_d06.salida.txt`): dos grafías «ALCALDIA MUNICIPAL DE RIOS NEGRO - NARE» y
«… RIOS NEGRO NARE» → `claveCanonica` idéntica («alcaldia municipal de rios negro nare»); el perfil
de hoy las parte en **2 filas (4 y 2)**: confirmada la premisa de A2 § 2.

**2) Verdad — un defecto reproducido que la ficha no ve.** El «N» del perfil y el «ganados» de la
fila que se pulsa en «Quién gana aquí» **no usan el mismo predicado**: el perfil cuenta con
`esAdjudicado` (`competencia_detalle.js:581`; `indice_competencia.js:186-194`) y la tabla de la
entidad con `cuentaParaCompetencia` (:222; `indice_competencia.js:253-260`, que exige
`desenlaceDe === "adjudicado"`). Una fila con estado y fase «Desierto» pero `adjudicado=Si`, ganador
y valor (lotes: la memoria R4 lo declara real) da `desenlaceDe=desierto_con_adjudicacion ·
esAdjudicado=true · cuentaParaCompetencia=false`. Ejecutado con `detalleEntidad` real: la fila de A
en «Quién gana aquí» dice **5**; `entidades[]` del perfil, agrupado con `claveCanonica`, dice
**6**. Un clic después, dos cifras distintas de lo mismo. Hoy ya pasa (5 en la fila; 4 + 2 en la
tabla del perfil) pero D-06 lo sube a una cabecera. No es un error del árbol: la memoria R4 dejó
`esAdjudicado` en el perfil a propósito («sacarlo del perfil INFRAVALORARÍA lo que esa empresa
ganó»). Se demuestra además que N(perfil) ≥ ganados(fila) siempre: toda fila del top de la entidad
lleva `clave` (ganador real o NIT), luego `adjudicacionAfirmada` y `esAdjudicado` son ciertos.
Condición: N y M salen de `detalleAdjudicatario` (mismo predicado entre sí y con la tabla del propio
modal), y la diferencia con la fila pulsada se DICE («1 figura como desierto con ganador publicado»)
publicando por entidad el conteo de `desierto_con_adjudicacion`, o se decide un solo predicado con
la memoria R4 delante y se marca SUPERADA; la prueba fija N − ese conteo === ganados de la fila.
- Empate de grafías: `nombreOriginal` lo decide el orden del corpus (memoria «Identidad de la
  entidad…», fixture «3 + 1, no 2 + 2»). Condición: desempate declarado (la grafía que el modal de la
  entidad ya publica como `entidad`, :489) y fixture 3+1.
- La forma de `entidades[]` cambia → `claveCache` v7 (`competencia_detalle.js:136`) sube a v8; sin
  eso, una hora de respuestas con la forma vieja tras desplegar (R11 en :131-135).
- «La tarjeta pasa `data-entidad`» (`app.js:1615`) lleva el nombre CRUDO; el perfil se abre desde
  el modal de la entidad con `cargarAdjudicatario(clave, nombre)` (`app.js:3384-3396`), que no recibe
  entidad: hay que llevar `entidad_normalizada` desde `pintarDetalle` hasta ahí (variable del modal o
  `data-entidad-clave` en las filas de `bloqueAdjudicatarios`).

**3) Memoria.** «Identidad de la entidad: dos formas de confundir a dos entidades (ago 2026)»
—una sola definición de entidad; D-06 la respeta si LLAMA a `claveCanonica` en el servidor— y
«Remates «R4-remates-inteligencia» de la ola 2 · B9a-H1/H2/H3, B9b-H1/H2/H3/H4/H5 (6-sep-2026)»
—`esAdjudicado` en el perfil es deliberado; D-06 no puede cambiarlo en silencio—.

**4) Costo — clic, real.** La agrupación es en el mismo barrido de `op=competidor`; ningún comando
más. Cerraduras que siguen: `tests/e2e.js:12924-12925` (`entidades.length === 1`,
`entidades[0].entidad === "INSTITUTO DE OBRAS DEL PORVENIR"`) pasan si `entidad` se conserva.
Cerradura nueva obligatoria: dos grafías → 1 fila (mutación: sin `claveCanonica` vuelven a ser 2).

**5) Lenguaje.** «En esta entidad: 6 de sus 15 contratos» pasa la cerca.

**6) ¿Ya existe?** La regla de identidad existe (`claveCanonica`) y el nombre más frecuente también
(:283-285): el plan la llama, no la copia. El dato «N de sus M» no se enseña hoy en ningún sitio.

**Veredicto: con condiciones.** (1) `claveCanonica` en el servidor, `clave` publicada por fila y
comparación con `entidad_normalizada`; (2) N y M del mismo predicado y la diferencia con la fila
(reproducida: 6 frente a 5) declarada o resuelta con la memoria R4 delante; (3) desempate de
grafías declarado, fixture 3+1; (4) caché v8; (5) transporte de la entidad de origen hasta
`cargarAdjudicatario`.

---

## D-07 · Las filas de «Quién gana aquí» dicen que se pueden pulsar — CON CONDICIONES

**1) Fuente — existe.** `detalleEntidad.adjudicatarios.top[].clave` (`competencia_detalle.js:423`)
y `bloqueAdjudicatarios` (`app.js:2918-2975`).

**Ejecutado** (`v_d07.salida.txt`, función real con el Pulso real y el fixture de la suite):
- La fila: `<tr class="… cursor-pointer …" data-adjudicatario="nit:900" data-nombre="ALFA SAS"
  title="Ver en qué otras entidades gana">` (`app.js:2929-2931`); sin `role`, sin `<button>`. Texto
  VISIBLE (sin etiquetas ni atributos, como `verA9` en `tests/e2e.js:8680`): «ALFA SAS NIT 900 6
  600000000 2026-01-15» — ninguna palabra dice que se puede pulsar. Confirmado.
- `data-adjudicatario=` en el bloque: **5**; rótulo del pliegue: «Ver los 5 adjudicatarios que más
  ganan» (`app.js:2967`); con `top.length === 1`: «Ver el adjudicatario que más gana» (rama singular).
- Segmentos de la barra (`pulso.js:398-399`): `<div class="absolute top-0 h-full" … title="ALFA SAS:
  6 (15 %)">`; **sin `data-*`**. «Otros»: leyenda «Otros (4) · 25», `title` «Otros: 25 (63 %)»: no
  explica que es la cola. Confirmado.
- Sin concentración (menos de 5 con ganador): sin `<details>`, tabla a la vista y las filas igual
  de mudas (A2 § 1.3 confirmado).
- El listener (`app.js:3392-3396`) usa `closest("[data-adjudicatario]")`: un segmento con ese
  atributo abriría el perfil sin tocar el listener.

**2) Verdad.** No hay cifra nueva. Tres hermanos vivos:
- **`tests/e2e.js:13035`** exige exactamente 5 `data-adjudicatario=` («las cinco filas siguen
  abriendo el perfil»): si los segmentos llevan el atributo (3 con tono propio) la cuenta pasa a 8 y
  la cerradura cae. La ficha solo nombra `:13033`. Condición: reescribir también `:13035` (o usar
  otro atributo para los segmentos y ampliar el listener).
- Solo 3 de los 5 del top tienen segmento propio: el 4.º y el 5.º van DENTRO de «Otros» (plegado por
  falta de tonos, `pulso.js:375`; memoria B9a). Anchos con el fixture a 320 px (`pulso.js:397`):
  48 · 40 · 32 · 200 px; con un líder del 5 %, 16 px, por debajo del **suelo táctil de 24 px** que la
  memoria fijó por regla. Condición: la barra es puerta secundaria; la fila con texto es la
  principal; «Otros» no es pulsable y su explicación no puede decir que la cola está «fuera de la
  tabla» (dos de sus miembros están en ella).
- `Pulso.apilada` es una primitiva compartida (`app.js:2957`, `:4334`, `:8850`, `:8887`): el
  atributo por segmento entra como opción declarada por el llamador (como `esCola`), no como una
  segunda barra.
- Rama singular: el rótulo nuevo necesita su forma para `top.length === 1` («Ver quién más gana y
  dónde más gana», pasa la cerca).
- La «cerradura sin tooltip» `tests/e2e.js:8663-8680` (`bq51`) NO cubre `bloqueAdjudicatarios`: cubre
  `leerJson`, `botonGuardar`, `notaUnidadHtml`, `pintarApu`, `configuracion`, `indicadores`,
  `origenMapeo` y el 503. Es el principio, no una cerradura de esta fila: el plan tiene que añadir la
  fila a un censo «lo que un teléfono VE».

**3) Memoria.** «Lote «B9a-entidad-graficos» de la consultoría del 4-sep · M-DGF-06, M-DGF-10
(6-sep-2026)»: el pliegue («la tabla —lo que se TOCA— pasa a un `<details>`») se conserva, y sus
cerraduras nombran «`<details>` con su rótulo» y «cinco `data-adjudicatario`»: las dos se reescriben
a propósito. «Estética premium y experiencia de uso · las 24 mejoras del encargo 2, implementadas
(5/6-sep-2026)»: «Suelo táctil de 24 px por regla» y «`title`: en el teléfono no hay tooltip» — es la
regla que D-07 invoca y la que limita los segmentos.

**4) Costo — clic, real.** Solo presentación sobre `op=entidad`; ninguna petición nueva.

**5) Lenguaje.** «Ver los 5 que más ganan y dónde más ganan», «Ver dónde más gana ›» y la propuesta
para «Otros» («Otros: los demás ganadores, sumados; no es una empresa») pasan la cerca.

**6) ¿Ya existe?** El camino existe (fila → `cargarAdjudicatario`, `app.js:3392-3396`); lo que falta
es la afordancia. No hay nada que llamar.

**Veredicto: con condiciones.** (1) reescribir `:13033` y `:13035`; (2) rama singular del rótulo;
(3) segmentos como puerta secundaria, «Otros» no pulsable y bien explicado (4.º y 5.º están en la
tabla), suelo táctil de 24 px; (4) el atributo por segmento como opción de la primitiva compartida;
(5) la fila entra en un censo «sin tooltip».

---

## D-08 · Campo `alcanzable_con_socio` de la fila — CONFIRMADO

**1) Fuente — existe.** `socioQueAlcanza` (`lib/socio_por_proceso.js:282-295`): «el PRIMER socio que
alcanza» en el orden de `pluralesDe` (:261-279) sobre `CANDIDATOS_CONSORCIO = ["genesis","prodiac"]`
(`lib/perfiles.js:357`). Lo publica `listar.js:920` solo para el dueño (`socioAplica`, :554), tomado
de la cascada (`:557 alcanzaConSocio = (l) => conSocio.get(l) || null`; `lib/filtros.js:693-696`,
`:723`, `:736`, `:755`, devuelto en `:762`). El recomendado es `socioPorProceso` →
`opciones.sort` (:384-388) → `recomendacion.socio` (:396-403).

**Ejecutado** (`v_d08.salida.txt`, módulos reales, perfiles reales):
- Pavimento $8.500 M (fixture `tests/e2e.js:4701`): `alcanzable_con_socio =
  {"socioId":"genesis","nombre":"Génesis Ingeniería y Construcción GIC SAS"}` y
  `socio.recomendacion = con_socio · socio=prodiac · cierra_todo=true` (opciones: prodiac(cierra),
  genesis(cierra)). **Por la cascada real** (`filtrarProcesosVisibles(...).conSocio.get(fila)`) sale
  el mismo objeto Génesis: es exactamente lo que `listar.js:920` publica. Dos «con quién» distintos en
  la misma fila: reproducido, como decían A3 § (b) y DUE-56.
- Puente $20.000 M: coinciden (solo PRODIAC cierra). Puente $200.000 M: `alcanzable_con_socio=null`
  y `socio = con_socio · cierra_todo=false`: dos predicados distintos («cierra todo» frente a «el
  mejor aunque no cierre»), no una contradicción, pero refuerza que no son la misma cosa.
- Nota de método: llamado en directo, `socioQueAlcanza` devuelve Génesis incluso donde el dueño
  alcanza solo (placa huella $300 M); en el listado eso no viaja porque la cascada solo guarda
  `conSocio` para filas RESCATADAS (`filtros.js:755`) → `null`. Las cifras que valen son las de la
  cascada, que el script imprime al lado.

**2) Verdad.** Retirar el nombre no convierte nada en cero ni promete nada: el rescate sigue siendo
booleano (`listar.js:663 || !!alcanzaConSocio(l)`; `diagnostico.js:261` también lo usa como `!!`).
La tarjeta no lo lee: `bloqueSocio` (`app.js:2008-2014`) lee `l.socio.tipo/cierra_todo`. El comentario
de `listar.js:917-919» («si el listado la dejó pasar, la tarjeta puede decir por qué») describe un
uso que no existe: se retira con el campo.

**3) Memoria.** «El veredicto de socio se lee AL GUARDAR, y la tarjeta queda en una línea
(11-sep-2026)»: lo que la fila «sí sigue llevando» son `tipo` y `cierra_todo` (30 B), sin
`alcanzable_con_socio`: D-08 la cumple mejor. Su párrafo «Lo que no estaba cerrado y ahora sí» dice
que `alcanzable_con_socio` ganó cerradura: el árbol no la tiene (censo ejecutado: la cadena solo
aparece en `listar.js:920`; en `tests/` no hay ninguna aserción; la cerradura de la fila fija las
claves de `resumenSocio`, `tests/e2e.js:4705-4706`). Condición implícita para el plan: la retirada
lleva su propia cerradura (mutación: volver a publicarlo). «Un proceso que se alcanza con socio ya no
se esconde (11-sep-2026)»: la cascada debe seguir diciendo CON QUIÉN en su mapa `conSocio`
(`tests/e2e.js:4620-4621` exige `socioId`) — D-08 no toca la cascada, solo lo que viaja en la fila.
«Con cuál de mis socios conviene ESTE proceso · `lib/socio_por_proceso` (11-sep-2026)»: el orden de
preferencia que hace divergir a los dos campos es deliberado y no se toca.

**4) Costo — petición, real.** Se calcula por fila en la cascada de `op=listar` y se publica por fila.
`listar.js:557` lo toma memoizado (`conSocio.get`): retirarlo ahorra BYTES, no CPU. Medido en UTF-8
con su clave: `"alcanzable_con_socio":null` = 27 B (+1 de la coma); Génesis = 100 B (+1); PRODIAC =
68 B (+1). El «−4 a −77 B» de la ficha no cuadra con ninguna medición: son **−28 a −101 B por fila**.

**5) Lenguaje.** No se pinta nada. Sin textos.

**6) ¿Ya existe?** El único «con quién» que debe quedar es `socioPorProceso` (ya existe, se llama, no
se reescribe). Nada más lee el campo (`public/`, `tests/`, `api/`, `lib/`: censo ejecutado).

**Veredicto: confirmado**, con la cifra de «afecta» corregida y una cerradura propia (mutación:
publicarlo otra vez debe caer).

---

## Anexo · comandos ejecutados

- `node tests/mapa.js detalleAdjudicatario | competencia_detalle | socioQueAlcanza | alcanzable_con_socio |
  claveCanonica | procesos_con_valor | socio_por_proceso | "perfil del competidor" | tooltip | "NO SE ESCONDE" |
  sinSocios | modalidad_competitiva` — coordenadas y `sed` de la memoria (secciones leídas: 783-809,
  9485-9620, 9621-9791 por grep, 10134-10296 por grep y 10168-10180 / 10270-10296, 11916-11989,
  12079-12134, 12390-12448, 7580-7596).
- `node tests/e2e.js --indice | grep …` → bloques: `unidad adjudicatario` (L 1987), `unidad socio por
  proceso` (L 4533), `unidad detalle de competencia` (L 4801), `unidad índice de competencia` (L 4838),
  `unidad pantalla · public/app.js sin tooltip` (L 8663).
- `node v_d05_d06.js` (código 0) · `node v_d07.js` (0) · `node v_d08.js` (0) · `node v_lenguaje_d05_d08.js`
  (0): salidas en `*.salida.txt`.
- Censo de lectores: `grep -rn "alcanzable_con_socio|socioQueAlcanza" lib/ public/ api/ tests/ docs/`
  (solo `lib/`: `filtros.js:695-696`, `listar.js:920`, `diagnostico.js:261`, `socio_por_proceso.js:282,416`)
  y el barrido recursivo del script (`listar.js:920` única aparición del campo).
- `git -C /home/user/portafolio-estrategico status --short` → vacío.

**No verificable desde aquí**: el comportamiento del `title` en un teléfono real y el ancho real de
los segmentos con nombres largos (se calcula con la regla de 320 px de `pulso.js:397`; navegador real
obligatorio para `public/` según CLAUDE.md); cuántas filas `desierto_con_adjudicacion` hay en el
histórico de producción (la memoria R4 lo deja para `/api/diagnostico` tras reconstruir el índice).
