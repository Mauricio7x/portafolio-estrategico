# Verificación adversaria · D-17 a D-24 (13/14-sep-2026)

> Para: sesión · Estado: informe fechado · Sustituido por: —

Reproducciones ejecutadas: `plan/v_d17_d24.js` (salida en `v_d17_d24.salida.txt`) y `plan/v_d17_baja.js` (salida en `v_d17_baja.salida.txt`). Ningún archivo del repositorio se tocó (`git status --short` vacío al cerrar). Anclas ruta:línea del árbol en main.

## D-17 · Cuánto bajan aquí, con la banda y con su origen — CON CONDICIONES
- FUENTE: `bajaDeMercado` (`lib/indice_baja.js:885-934`) devuelve `baja_mediana, baja_p25, baja_p75, procesos_contados, granularidad_utilizada, modalidad_utilizada, mensaje` (líneas 905-920); viaja como `baja_mercado` (`lib/handlers/procesos/listar.js:858`, memoizado por fila en :466-471). `chipBaja` (`public/app.js:1349-1366`) pinta solo la mediana y los pesos («unos $…»); la banda y el origen quedan en `title`. Corto del glosario: «Suelen bajar» (`public/glosario.js:58`).
- VERDAD (reproducido, `v_d17_baja.js`): n=8 → mediana 7, p25 3, p75 9, mensaje con «8 contratos ya adjudicados»; n=3 → `nivel:"sin_dato"`, `procesos_contados:3`, «Sin base suficiente: hacen falta 5 procesos adjudicados y hay 3» (el conteo bajo el mínimo SÍ viaja); **IQR cero** (p25=mediana=p75=35, n=57, el caso real de MEMORIA «La dispersión de la baja se MIDE») pasa `utilizable` (:802-811) y la banda diría «la mitad bajó entre 35 % y 35 %»; **hash viejo sin p25/p75** → null (no se puede pintar banda); **mediana 0** → `mensajeDe` dice «Aquí se gana sin bajar el precio» (:838-841): «Suelen bajar 0 %» sería la instrucción absurda que esa función ya evita.
- ORIGEN «departamento»: la cascada que DECIDE llega a `departamento_familia` (:896) —reproducido «solo departamento_familia»: `baja_mercado` sin_dato y `baja_departamento` con mediana 4 y frase de HECHO («En TOLIMA los que ganaron descontaron cerca de 4%…»)—. MEMORIA «Lote B9b» mide 0 grupos de `departamento_familia` en producción: la variante «Suelen bajar 4 % · lectura del departamento» solo puede salir de `baja_departamento`, que por decisión escrita NO es instrucción de precio («dos instrucciones de precio en la misma tarjeta se contradirían»). Rotularla «Suelen bajar» la convierte en la segunda instrucción.
- YA EXISTE la banda en palabras: `htmlBajaAdjudicatario` «entre 6 % y 10 %» (cerradura `tests/e2e.js:13086-13089`) y la franja p25–p75 de Piso/Techo (MEMORIA M-DGF-01). Lo que decide el precio es `baja_para_precio` (encogida, `listar.js:606`); la tarjeta enseña la medida (`baja`), y así lo declara el comentario :602-605.
- COSTO petición: correcto (por fila en op=listar; el índice es nocturno).
- LENGUAJE: «lectura del departamento» es voz interna. Propuesta: reutilizar `baja_departamento.mensaje` tal cual (ya es hecho + n), sin el rótulo «Suelen bajar».
- Condiciones: (1) banda solo si `baja_p25 != null && baja_p75 != null && baja_p75 > baja_p25` (misma guarda que `multiplicadorPrecio`); (2) mediana ≤ 0 → palabras de `mensajeDe`, nunca «Suelen bajar 0 %»; (3) la tercera variante sale de `baja_departamento.mensaje` y no lleva «Suelen bajar»; (4) n bajo el mínimo: «sin datos» + `mensaje` (hacen falta 5 y hay 3) — ya viaja; (5) sin token `lib/publico.js:133` anula `baja_mercado`: el chip dice «sin datos» donde en realidad «no se muestra sin credencial» (comportamiento previo, no de este dato; se declara).

## D-18 · Cuantía, y su ausencia dicha — CON CONDICIONES
- FUENTE: `cuantia_cop = primerNumero(...) || 0` (`lib/negocio.js:207`), reproducido: sin precio_base → `cuantia_cop:0, cuantia_rango:null, puntaje_ponderado:NaN` (JSON → null). La tarjeta ya dice «Cuantía no publicada» (`public/app.js:2152-2157`, sin `|| 0`).
- YA EXISTE la segunda frase: la puerta P2 sin cuantía devuelve `pasa:true, sin_dato:true`, mensaje «El proceso no publica cuantía: la capacidad K no se puede verificar. Confírmela en el pliego.» (`lib/puertas.js:121,222`, reproducido) y su redacción pública «…la capacidad de contratación no se puede verificar. Confírmela en el pliego.» (`lib/publico.js:77`). D-18 debe LLAMAR ese mensaje, no reescribir «sin ella no sabemos si le cabe».
- A1-H5 confirmado: `valorEsperado` → 0 sin cuantía (`lib/probabilidad.js:470-475`, reproducido) y `atractividad: ctx.ve || 0` (`listar.js:153`). PERO `valorEsperado({cuantia_cop:1e9}, 0)` también da 0: el «grupo propio al final» tiene que decidirse con `presupuestoOficialDe(l) === null` (`lib/negocio.js:201-204`, reproducido: `{cuantia_cop:0}` → null), no con `ve === 0`.
- MEMORIA «Auditoría integral del 1-sep-2026…»: «`cuantia_cop` sigue viajando 0 sin cuantía (el cambio a null cruza a docenas de consumidores; las puertas ya lo tratan como sin dato)». D-18 no puede cambiar el 0 del cable sin un censo de consumidores; el rótulo se decide en la tarjeta y en el orden con la regla existente.
- COSTO: `cuantia_cop` es sync (enriquecer en ingesta); el reagrupado del orden corre por petición en `listar.js` (ORDEN_CAMPOS) — declarar «sync» es incompleto pero no falso.
- LENGUAJE: bien; sustituir «sin ella no sabemos si le cabe» por el texto de `publico.js:77`.

## D-19 · Rótulo «cuantía alta / media / baja» — CONFIRMADO
- `RANGO_CUANTIA` (`public/app.js:2131`) y el único renglón que lo pinta (:2156), solo cuando hay cuantía. Umbrales `lib/negocio.js:34-35`; `cuantia_rango` null sin cuantía (:208-212, reproducido). El filtro sigue en el servidor (`listar.js:513`, :638). Cerraduras: `e2e.js:10039` (valores del campo servido), :25020 (`rangoCuantiaDe(0)` null), :16391 (fixture) — ninguna exige el rótulo en la tarjeta. Retirar :2156 y conservar el campo no rompe nada medido. Costo sync correcto (enriquecer).

## D-20 · Banda de competencia — CON CONDICIONES
- `bandaCompetencia` (`public/app.js:1606-1621`): texto `${titulo} · ${promedio} en ${procesos}`; títulos y `●` en `COMPETENCIA_ENTIDAD` (:551-556), incluido «Sin datos históricos de esta entidad». Cerraduras :23608-23614 (data-entidad, cursor-pointer, hover:underline) y :23348-23358 (`conBase`) se conservan si solo cambia el texto. «Quién gana aquí» ya es el título del modal (`e2e.js:23566`).
- SEGUNDA COPIA del mismo texto en el servidor: `badgeCompetencia` (`lib/handlers/perfil/resumen.js:125-133`) «Poca competencia — promedio 1,4 oferentes en 55 procesos», con cerradura `e2e.js:16344`. Si la tarjeta deja de repetir la cifra y el panel sigue diciéndola, un mismo hecho tiene dos redacciones. Y `avisoCompetencia` (`app.js:1465-1475`) repite el promedio (D-71 lo conserva).
- COSTO petición correcto (`competenciaDe` por fila, `listar.js:461-463`). Lenguaje correcto.
- Condiciones: decidir el badge del panel en el mismo cambio; «55 procesos» = `total_procesos` con `conBase` (sin base no hay cifra).

## D-21 · Cierre prorrogado, con los días — CON CONDICIONES
- FUENTE: `_versiones`, `_cierre_prorrogado`, `_cierre_inicial` (`lib/almacen.js:365-378`), activados con `senales:true` en `listar.js:255`; `sinAdjudicacion` los conserva (reproducido) y ya llegan a la tarjeta (`app.js:2177` chip «Cierre prorrogado»; :1835). Factor `FACTOR_CIERRE_PRORROGADO = 1.20` (`lib/probabilidad.js:126`, aplicado en :349). B3: `sync.js:576-578` persiste `cierre_prorrogado/versiones_vistas/fecha_cierre_inicial` al histórico.
- REPRODUCIDO: 3 versiones con cierre 1-oct → 13-oct: `_cierre_prorrogado:true`, `_cierre_inicial:"2026-10-01T17:00:00"`, 12 días. Cierre ADELANTADO (13 → 1): prorrogado false, inicial = vigente, 0 días (coherente). **Primera versión sin fecha de cierre** (P-5): chip false y 0 días, pero `evaluarAdendas` dice «pasó de — a 13/10/2026. Se prorrogó: hay más tiempo…» (`lib/adendas.js:63-65`): dos lecturas contrarias en la misma tarjeta. **Formatos mezclados** (fecha sola vs fecha-hora): 12,708 días — hay que truncar/redondear y declararlo.
- COSTO petición correcto. `_versiones > 1` es redundante con `_cierre_prorrogado`.
- LENGUAJE: la tarjeta escribe fechas «1 oct 2026» (`app.js:2136`); «1 de oct» mezcla estilos. Propuesta: «Cierre prorrogado 12 días: antes cerraba el 1 oct 2026 (desde que la aplicación sigue el proceso)».

## D-22 · Cambio de reglas, con conteo y fecha — CON CONDICIONES (el conteo y la fecha no son lo que el corto promete)
- FUENTE: `_cambios` (`almacen.js:379-386`) compara SOLO la versión más vieja y la más nueva (5 campos); `evaluarAdendas` (`adendas.js:44-102`) devuelve `n` = renglones de campo que cambiaron. REPRODUCIDO: 3 versiones → `n:2`; 3 versiones con un solo campo cambiado → `n:1`. «2 cambios» se lee como «cambió dos veces» y el dato no lo sostiene; lo que cuenta veces es `_versiones` («se reescribió 3 veces»).
- FECHA: la traza guarda `u` (`:updated_at`) de primera/última pero no lo expone; la fila deduplicada conserva `:updated_at` (`lib/proyeccion.js:39`, en CAMPOS, viaja) = fecha de la última VERSIÓN, no del último cambio (P-4: cambio el 3-sep, `:updated_at` 10-sep). «El último el 3 de septiembre» no es calculable con exactitud; sí lo es «visto por última vez el 10 sep».
- YA EXISTE `bloqueAdendas` (`app.js:1045-1052`) con `resumen` + renglones; fuente declarada en `adendas.js:100`. Costo petición correcto (`listar.js:911`).
- MEMORIA «Fases 4 y 5 del plan v3 · Guardián del Formulario 1 y vigía de adendas (ago 2026)»: «solo ve cambios del dataset» (coincide con la ficha).

## D-23 · Cómo lo adjudican, arriba — CON CONDICIONES
- FUENTE: `modalidad_de_contratacion` en la proyección (A4 línea 26; viaja); chip plegado exacto en `app.js:2201`; `glosario.js:81` «Cómo lo adjudican»; :77-80 fija que la modalidad se conserva como NOMBRE PROPIO y da `corto: "Menor cuantía · avise antes"`. `chipManifestacion` ya va arriba (:2174).
- VERDAD: «tal como la publica p6dx» pinta literales que varían («Seleccion Abreviada Menor Cuantia Sin Manifestacion Interes», «Licitación pública Obra Publica», `docs/datos.md § «6. Censo de columnas de `p6dx-8zbt` para los filtros (Fase 8 · 2026-08-16)»`); MEMORIA «Fase 8 · Los siete filtros» casa la modalidad por RAÍZ normalizada por eso. Existe `modalidadCanonica` (`lib/indice_baja.js:179-186`, raíces de `lib/filtros.js:223-226`).
- Condiciones: mover (no duplicar) el chip de :2201; pintar el literal con el nombre propio del glosario cuando la raíz case (`modalidadCanonica`) y el literal en `title`; sin modalidad, nada.
- COSTO sync correcto. Lenguaje correcto.

## D-24 · Precio global arriba — CON CONDICIONES
- FUENTE: `tipoPrecio` (`lib/negocio.js:264-272`), al servir (`listar.js:881`) → petición correcto. REPRODUCIDO: «PRECIOS UNITARIOS» → unitarios; «precio global fijo» → global; las dos → null; «cobertura global» → null; vacío/null → null. Catálogo cerrado `e2e.js:10988-10990`; la cerradura :23566 exige los literales «Precios unitarios», «Precio global», «tipo_precio» en app.js (conservarlos).
- YA EXISTE el texto propuesto: `lib/guia_proceso.js:528-529` «Es a precio global: el riesgo de las cantidades es suyo» con motivo «el objeto dice «precio global»»; títulos en `app.js:2202-2205`; también `lib/formulario1.js:84-86` y `lib/dictamen.js:479`. Llamar la redacción de `guia_proceso`, no escribir una cuarta.
- MEMORIA «La guía «Don Héctor» de cada proceso guardado y el dictamen en Mis procesos (3-sep-2026)»: forma de precio = `tipoPrecio`.
- Condición: arriba solo con `tipo_precio === "global"` y con el origen dicho («el objeto dice…»), porque es un estimado por regex sobre nombre + descripción.
